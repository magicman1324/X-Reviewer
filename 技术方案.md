# X-Reviewer — AI PR Review 助手 技术方案

---

## 一、项目概述

**X-Reviewer** 是一款面向 GitHub 协作流程的 AI 代码评审助手，以 GitHub App 形态运行。开发者正常提交 PR 后，AI 自动在评论区生成结构化审查报告，实现零门槛、零感知的代码评审体验。

---

## 二、核心技术栈

| 层级 | 技术选型 | 选型理由 |
|------|---------|---------|
| 开发语言 | TypeScript + Node.js | 类型安全，与 Probot 生态深度兼容 |
| 网关框架 | **Probot** | GitHub 官方 App 框架，内置 Webhook 签名校验、OAuth 鉴权、Token 自动刷新，免去手写基础设施 |
| AI 模型基座 | deepseek-v4-pro | 专精代码语义与 Diff 解析，超长上下文，低延迟，OpenAI 接口兼容 |
| 运行时 | Node.js 18+ LTS | 支持原生 fetch、ESM、异步队列 |
| 部署 | Docker + Cloud Run / 轻量 VPS | 无状态服务，水平扩展友好 |
| 可选存储 | Redis | 去重缓存 + 请求幂等 |

---

## 三、系统架构设计

### 3.1 整体架构图

```
┌──────────────┐     Webhook      ┌──────────────────────────────┐
│   GitHub     │ ────────────────> │        X-Reviewer            │
│   PR Event   │                   │                              │
│              │ <──────────────── │  Probot Webhook Handler      │
│  Comment     │    Update API     │       │                      │
└──────────────┘                   │      ▼                       │
                                   │  ┌─────────────────────┐    │
                                   │  │  Diff 净化管道       │    │
                                   │  │  - 过滤 lock 文件    │    │
                                   │  │  - 过滤二进制/图片   │    │
                                   │  │  - 提取纯业务代码    │    │
                                   │  └─────────┬───────────┘    │
                                   │            ▼                 │
                                   │  ┌─────────────────────┐    │
                                   │  │  异步任务队列        │    │
                                   │  │  (内存队列 / BullMQ) │    │
                                   │  └─────────┬───────────┘    │
                                   │            ▼                 │
                                   │  ┌─────────────────────┐    │
                                   │  │  AI 推理引擎         │    │
                                   │  │  - Prompt 构建       │    │
                                   │  │  - 上下文增强        │    │
                                   │  │  - 模型调用          │    │
                                   │  │  - 输出结构化解析    │    │
                                   │  └─────────┬───────────┘    │
                                   │            ▼                 │
                                   │  ┌─────────────────────┐    │
                                   │  │  评论发布/更新模块   │    │
                                   │  └─────────────────────┘    │
                                   └──────────────────────────────┘
```

### 3.2 核心设计亮点

#### 3.2.1 三秒异步解耦

GitHub Webhook 要求 10 秒内返回响应，超时会触发重试熔断。AI 推理通常需要 15~60 秒，因此采用异步解耦：

```
Webhook 到达 → 参数校验 → HTTP 200 立即返回 → AI 推理丢入异步队列
```

**关键实现：**

```typescript
// webhook handler
app.on('pull_request.opened', async (context) => {
  const payload = context.payload;
  // 立即返回 200，github 不再等待
  context.octokit.issues.createComment({
    owner: payload.repository.owner.login,
    repo: payload.repository.name,
    issue_number: payload.number,
    body: '🤖 *正在深度阅读代码，预计 30 秒内完成分析...*',
  });
  // 异步执行推理
  setImmediate(() => reviewQueue.enqueue(payload));
});
```

#### 3.2.2 占位更新机制

1. **T+0s**: 先发送占位评论，用户感知 AI 已开始工作
2. **T+30s**: AI 推理完成后，通过 GitHub `PATCH /repos/{owner}/{repo}/issues/comments/{comment_id}` 接口无缝替换占位内容，用户无跳变感知

#### 3.2.3 Diff 净化管道

降低 Token 消耗、减少 AI 误报：

| 过滤项 | 处理方式 | 收益 |
|--------|---------|------|
| `package-lock.json`、`yarn.lock` | 直接丢弃 | 减少 30% Token |
| 图片、二进制文件 | 文件扩展名匹配，丢弃 | 减少噪声 |
| 仅 import 变更（无逻辑变动） | 标记为 Lint 级，不送入深度分析 | 减少 10% 请求量 |
| 纯格式化/注释变更 | 正则匹配剥离 | 减少误报 |

**处理后的 Diff**：仅保留 `+` / `-` 行的真实业务代码变更，附带前后 3 行上下文。

---

## 四、Prompt 工程设计

### 4.1 System Prompt（核心）

```
你是一位资深代码审查专家。你的任务是对 GitHub Pull Request 的代码变更
进行严格且准确的审查。

## 审查原则
1. 只分析 diff 中实际变更的代码行
2. 结合代码上下文理解修改意图
3. 区分"真正的问题"和"风格偏好"
4. 每个风险必须附文件路径和行号
5. 输出格式必须是合法的 JSON

## 风险分级
- critical: 🔴 高危 — 安全漏洞、未捕获异常、数据泄漏、SQL注入、
  竞态条件、空指针未防护
- warning: 🟡 警告 — 死代码、冗余变量、性能隐患、可能的边界情况未处理

## 输出要求
- 总结必须用一句话概括修改目的
- 每个风险必须给出可执行的修复代码
- 不做代码风格的评论（交给 ESLint/Prettier）
```

### 4.2 User Prompt 模板

```
请审查以下 Pull Request 变更：

## PR 标题
{pr_title}

## PR 描述
{pr_body}

## 变更文件列表
{changed_files}

## 代码 Diff（已过滤）
{diff_content}

## 关联上下文（如有）
- 关联 Issue: {linked_issues}
- 项目语言: {detected_language}
- 框架: {detected_framework}

请输出 JSON 格式的审查报告。
```

### 4.3 输出格式约束（JSON Schema）

```json
{
  "summary": "一句话总结修改目的与影响范围",
  "risks": [
    {
      "level": "critical|warning",
      "file": "src/services/audio.ts",
      "line": 42,
      "title": "getUserMedia 未捕获异常",
      "description": "用户拒绝麦克风权限时将导致未处理 Promise rejection",
      "suggestion": "包裹 try...catch 并提供降级方案",
      "fix_code": "try {\n  const stream = await navigator.mediaDevices.getUserMedia(...)\n} catch(e) {\n  handleError(e);\n}",
      "is_false_positive_likely": false
    }
  ],
  "overall_score": 7.5,
  "suggested_labels": ["bug-risk", "needs-test"]
}
```

---

## 五、上下文获取策略

### 5.1 上下文获取层级

| 层级 | 获取内容 | API | 成本 |
|------|---------|-----|------|
| L0 | PR Title + Body + Diff | `GET /repos/{owner}/{repo}/pulls/{number}` | 1 次请求 |
| L1 | PR 关联 Issue | `GET /repos/{owner}/{repo}/issues/{number}/timeline` 过滤 cross-reference 事件 | 1~2 次请求 |
| L2 | 被修改文件的完整源码（非 diff 部分） | `GET /repos/{owner}/{repo}/contents/{path}?ref={head}` | N 次请求 |
| L3 | 项目技术栈识别 | `GET /repos/{owner}/{repo}/contents/package.json` 等 | 1~2 次请求 |
| L4 | 大文件截断 + 模块依赖图 | 组合 API + AST 解析 | 按需 |

**默认策略**：每 PR 调用 L0 + L1，Token 预算 ≤ 64K。若 diff 超过 64K，按文件优先级排序后截断（优先保留 `.ts`、`.js`、`.go`、`.py` 等主要语言文件）。

### 5.2 上下文优化

- **关联 Issue 注入**：若 PR Body 包含 `fixes #123`，拉取 issue #123 内容注入 Prompt，帮助 AI 理解需求背景
- **项目类型感知**：读取 `package.json` / `go.mod` 识别技术栈，加载对应领域的审查规则
- **文件分组**：将同一模块的多个文件变更打包送入同一推理请求，保持语义完整性

---

## 六、误报与漏报控制

### 6.1 误报控制策略

| 策略 | 实现方式 |
|------|---------|
| 白名单过滤 | ESLint/Prettier 能检测的问题（代码风格、格式），直接丢弃不报 |
| 置信度阈值 | 模型输出附带 `confidence` 字段，低于 0.7 的 warning 降级为 info |
| 变更范围限定 | 只分析 `+` / `-` 行及其上下文，不做全局代码审查 |
| 去重 | Redis 记录已审查的 (commit_sha, file, line_hash)，避免重复报告同一问题 |

### 6.2 漏报控制策略

| 策略 | 实现方式 |
|------|---------|
| 多维度审查清单 | Prompt 内置安全检查表：注入攻击、XSS、异常处理、资源泄漏、竞态条件 |
| 安全规则库 | 针对 SQL 拼接、`innerHTML` 赋值、`eval()` 调用等硬模式做正则前置检测 |
| 关键文件全量审查 | 标记 `middleware/`、`auth/`、`db/` 等安全敏感路径，送入更多 Token 预算 |

### 6.3 质量监控指标

- **采纳率**：AI 建议被标记为 `resolved` 的比例（目标 ≥ 60%）
- **遗漏检出率**：对比人类 Reviewer 的评论，AI 是否遗漏了同等严重级别的问题
- **误报率**：标记为 `wontfix` 或 `dismissed` 的 AI 评论比例（目标 ≤ 25%）

---

## 七、核心模块详细设计

### 7.1 目录结构

```
x-reviewer/
├── src/
│   ├── index.ts              # Probot App 入口
│   ├── handlers/
│   │   ├── webhook.ts        # Webhook 事件路由
│   │   └── commands.ts       # 斜杠命令（/review, /re-review）
│   ├── services/
│   │   ├── diff-pipeline.ts  # Diff 净化管道
│   │   ├── context-builder.ts# 上下文构建器
│   │   ├── ai-engine.ts      # AI 推理引擎
│   │   ├── prompt-builder.ts # Prompt 模板管理
│   │   └── comment-manager.ts# 评论发布/更新
│   ├── queue/
│   │   └── review-queue.ts   # 异步任务队列
│   ├── rules/
│   │   ├── security.ts       # 安全规则库
│   │   └── patterns.ts       # 危险模式正则
│   ├── utils/
│   │   ├── github-client.ts  # Octokit 封装
│   │   └── logger.ts         # 日志
│   └── types/
│       └── index.ts          # 类型定义
├── test/
├── Dockerfile
├── package.json
└── tsconfig.json
```

### 7.2 关键模块接口

```typescript
// types/index.ts
interface ReviewRequest {
  owner: string;
  repo: string;
  prNumber: number;
  title: string;
  body: string;
  headSha: string;
  baseSha: string;
  files: ChangedFile[];
  diff: string;
}

interface ReviewReport {
  summary: string;
  risks: RiskItem[];
  overallScore: number;
  suggestedLabels: string[];
}

interface RiskItem {
  level: 'critical' | 'warning';
  file: string;
  line: number;
  title: string;
  description: string;
  suggestion: string;
  fixCode?: string;
  confidence: number;
  ruleRef?: string;
}

// ai-engine.ts
class AIEngine {
  async analyze(request: ReviewRequest): Promise<ReviewReport>;
  private buildPrompt(request: ReviewRequest): ChatMessage[];
  private parseResponse(raw: string): ReviewReport;
  private validateReport(report: ReviewReport): boolean;
}

// diff-pipeline.ts
class DiffPipeline {
  filter(diff: string, files: string[]): FilteredDiff;
  private isNoise(filename: string): boolean;
  private extractBusinessLines(content: string): string;
  private truncateByPriority(files: ChangedFile[], maxTokens: number): ChangedFile[];
}

// comment-manager.ts
class CommentManager {
  async postPlaceholder(ctx: Context): Promise<number>;     // 返回 comment_id
  async updateWithReport(ctx: Context, commentId: number, report: ReviewReport): Promise<void>;
}
```

### 7.3 异步队列设计

使用 **BullMQ**（基于 Redis）或简化的内存队列：

```typescript
// review-queue.ts
import { Queue, Worker } from 'bullmq';

const reviewQueue = new Queue<ReviewRequest>('pr-review', {
  connection: redisConnection,
  defaultJobOptions: {
    attempts: 3,
    backoff: { type: 'exponential', delay: 5000 },
    removeOnComplete: { count: 100 },
    removeOnFail: { count: 50 },
  },
});

// Worker 处理
const worker = new Worker('pr-review', async (job) => {
  const request = job.data;
  const report = await aiEngine.analyze(request);
  await commentManager.updateWithReport(request.ctx, request.commentId, report);
}, { connection: redisConnection });
```

---

## 八、模型选择与对比

| 维度 | deepseek-v4-pro | Claude Sonnet 4.6 | GPT-4o |
|------|----------------------|-------------------|--------|
| 代码审查精度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐☆ |
| 上下文窗口 | 128K | 200K | 128K |
| 响应延迟（首 Token） | ~3s | ~2s | ~2s |
| 成本（每 PR） | ~$0.02 | ~$0.05 | ~$0.08 |
| API 兼容性 | OpenAI 兼容 | Anthropic SDK | OpenAI SDK |
| 中文支持 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐☆ |

**推荐方案**：以 **deepseek-v4-pro** 为主模型（成本低、代码能力顶级、中文友好），Claude Sonnet 4.6 作为仲裁模型（高复杂度 PR 或用户请求 `/deep-review` 时启用）。

---

## 九、安全与风险控制

### 9.1 安全措施

| 风险 | 对策 |
|------|------|
| Prompt 注入 | 对 PR 标题和描述中的指令性文本做转义处理，包装在 `<pr_content>` XML 标签内 |
| Token 泄露 | AI 请求不包含任何 API Key、Password 等敏感信息（Pre-filter 正则过滤） |
| 代码泄露 | 模型 API 调用使用 TLS，不记录 Diff 内容到日志，不带 Telemetry |
| 滥用防护 | 单仓库限频（每分钟 3 个 PR），避免恶意触发大量推理请求 |
| GitHub App 权限最小化 | 仅申请 Issues R/W + Pull Requests R/W + Metadata R 权限 |

### 9.2 容错设计

- **模型超时**：30 秒无首 Token 则 fallback 到更快的模型
- **解析失败**：AI 输出非 JSON 时，用正则兜底提取关键字段；完全失败则降级为纯文本评论
- **GitHub API 限流**：Octokit 自带 retry + conditional request，搭配本地缓存

---

## 十、使用体验设计

### 10.1 触发方式

| 触发方式 | 行为 |
|---------|------|
| PR 首次创建 | 自动触发全量审查 |
| PR 有新 commit 推送 | 增量审查（仅分析新增 commit 的 diff） |
| 评论 `/review` | 手动触发全量审查 |
| 评论 `/deep-review` | 开启增强模式，启用第二级模型 + 完整文件上下文 |

### 10.2 评论展示格式

遵循 PRD 定义的 UI Mock，使用 GitHub Markdown 格式化输出：

```markdown
## 🤖 X-Reviewer AI 代码评审报告

### 📝 PR 变更总结
本次修改在 `src/services/` 目录下引入了音频模块，通过 Web Audio API 进行音频流采集。

### ⚠️ 风险代码识别

| 级别 | 文件 | 行号 | 问题 | 建议 |
|------|------|------|------|------|
| 🔴 高危 | `src/audio.ts` | L42 | `getUserMedia` 未捕获异常 | 添加 try...catch |
| 🟡 警告 | `src/utils.ts` | L18 | 未使用的变量 `temp` | 删除或标注 `_temp` |

### 💡 修复建议

**`src/audio.ts:L42`**
\`\`\`typescript
try {
  const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
} catch (error) {
  handlePermissionError(error);
}
\`\`\`

### 📊 评审评分
综合评分: **7.5/10** — 核心逻辑正确，异常处理需加强。
```

---

## 十一、未来扩展方向

### 11.1 短期（1-3 月）

- **多仓库审查规则定制**：支持 `.x-reviewer.yml` 配置文件，仓库可按需定制审查规则
- **Review 历史学习**：基于人类 Reviewer 的 Resolve / Dismiss 反馈微调 Prompt 权重
- **Slack / 飞书通知**：高危问题即时推送至 IM 渠道

### 11.2 中期（3-6 月）

- **平台扩展**：支持 GitLab MR、Gitee PR，抽象统一的 VCS Adapter 层
- **代码质量趋势面板**：Dashboard 展示仓库代码质量变化趋势（每次 PR 的评分曲线）
- **基于 Embedding 的相似问题检索**：将历史 Review 向量化，新 PR 变更匹配历史相似问题

### 11.3 长期（6-12 月）

- **代码库全局知识图谱**：Build 项目的符号索引和依赖图，让 AI 审查时理解跨文件影响
- **自主修复 PR**：高危问题 AI 在 Review 的同时提交 fix PR，Reviewer 可直接合并
- **多模态审查**：审查 UI 代码时结合截图理解视觉变更

---

## 十二、开发排期（3 天）

| 阶段 | 时间 | 产出 |
|------|------|------|
| Day 1 | 上午 | Probot 项目脚手架搭建，Webhook 接收与校验，GitHub App 注册 |
| Day 1 | 下午 | Diff 净化管道、上下文构建器 |
| Day 2 | 上午 | AI Prompt 工程、模型对接、输出解析 |
| Day 2 | 下午 | 异步队列、占位更新机制、评论格式化 |
| Day 3 | 上午 | 端到端联调、误报/漏报人工测试、边界 case 覆盖 |
| Day 3 | 下午 | README 撰写、路演 PPT 准备、Demo 视频录制 |
